function repetirNumeroEmOrdemCrescente (numero) {
    let num = numero
    let contador = 0
    
    while(contador < num ) {
        contador++
        console.log(`${contador}`)
    }
}
repetirNumeroEmOrdemCrescente(50)